## WalletConnect (optional)
# Create a project at https://cloud.walletconnect.com and paste the Project ID here.
# If not set, WalletConnect connector will be hidden in the UI.
NEXT_PUBLIC_WC_PROJECT_ID=

## Network toggle
# Set to "true" to run against Mantle Sepolia testnet (chainId 5003) instead of Mantle mainnet.
NEXT_PUBLIC_USE_MANTLE_SEPOLIA=false

## ERC-4337 (Option B) - Safe deployment via Bundler (required for "Deploy vault/account")
# Bundler RPC URL for Mantle (ERC-4337). Provide your bundler endpoint here.
# Example (provider-specific): https://.../rpc?apikey=...
NEXT_PUBLIC_BUNDLER_RPC_URL=

# Paymaster endpoint (gas sponsorship). When set, deploy uses sponsored UserOperations.
NEXT_PUBLIC_PAYMASTER_RPC_URL=



