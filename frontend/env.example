## WalletConnect (optional)
# Create a project at https://cloud.walletconnect.com and paste the Project ID here.
# If not set, WalletConnect connector will be hidden in the UI.
NEXT_PUBLIC_WC_PROJECT_ID=

## Network toggle
# Set to "true" to run against Mantle Sepolia testnet (chainId 5003) instead of Mantle mainnet.
NEXT_PUBLIC_USE_MANTLE_SEPOLIA=false

## ERC-4337 (Option B) - Safe deployment via Bundler (required for "Deploy vault/account")
# Bundler RPC URL for Mantle (ERC-4337). Provide your bundler endpoint here.
# Example (provider-specific): https://.../rpc?apikey=...
NEXT_PUBLIC_BUNDLER_RPC_URL=

# Paymaster endpoint (gas sponsorship). When set, deploy uses sponsored UserOperations.
NEXT_PUBLIC_PAYMASTER_RPC_URL=

## Token addresses
# USDC address on Mantle mainnet (chainId 5000)
NEXT_PUBLIC_USDC_ADDRESS_MANTLE=

# USDC address on Mantle Sepolia (chainId 5003)
NEXT_PUBLIC_USDC_ADDRESS_MANTLE_SEPOLIA=

## Testing
# For testing on Mantle Sepolia you can deploy the mock token `ARBITER_USDC` from /contracts
# and set NEXT_PUBLIC_USDC_ADDRESS_MANTLE_SEPOLIA to that deployed address.

## Safe modules (optional)
# ProofGateSafeModule address deployed on the target chain.
# When set, newly deployed Safes will have this module enabled during setup.
NEXT_PUBLIC_PROOF_GATE_SAFE_MODULE_ADDRESS=

## Agent EOA address (required for agent activation)
# The agent is an EOA (Externally Owned Account) wallet address controlled by the Arbiter service.
# This is the wallet that will call ProofGateSafeModule.executeWithProof() to execute trades.
# All users enable the same agent EOA, but with different policy hashes that encode their preferences.
# The "agent type" (conservative/balanced/aggressive) is encoded in the policy hash, not as a separate agent.
NEXT_PUBLIC_AGENT_ADDRESS=



